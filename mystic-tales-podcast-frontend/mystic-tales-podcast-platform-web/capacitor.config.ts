import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.localhost.mystic',
  appName: 'mystic-tales-podcast-platform-web',
  webDir: 'dist'
};

export default config;
